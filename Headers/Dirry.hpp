#include <string>


namespace TrickyUnits{
	using namespace std; // save, even in a header. This using only affects the namespace scope! :P

	string Dirry(string path);
	void DirryVar(string var, string value);
	string DirryVar(string var);

}
